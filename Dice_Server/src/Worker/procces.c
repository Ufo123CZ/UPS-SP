#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "procces.h"

void proccesInput(char *buffer) {
    printf("Input: %s\n", buffer);
}

void proccesOutput(char *buffer) {
    printf("Output: %s\n", buffer);
}
